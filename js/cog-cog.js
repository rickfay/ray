/**
 * COG Base Object
 */
cog.Class.define("Cog", null, {

    /**
     * Construct the Component
     */
    construct: function construct() {
        console.warn(`No constructor defined for ${this.getClassName()}`);
    }
});